<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gd.lms.mapper.LectureMapper">
	<!-- 특정 강사가 강의하는 lecture의 리스트 -->
	<select id="selectLectureListByTeacher" parameterType="String" resultType="Map">
		SELECT 
		l.lecture_name lecture_name
		,l.lecture_start_date lectureStartDate
		,l.lecture_end_date lectureEndDate
		,ls.subject_name subjectName
		FROM lecture l 
		INNER JOIN lecture_subject ls 
		USING(lecture_name) 
		WHERE l.lecture_active='개설승인' AND l.teacher_name = #{teacherName}
	</select>
</mapper>